import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "app.john.pastor",
  appName: "JOHN AI",
  webDir: "dist",
  server: { androidScheme: "https" }
};

export default config;
